Connecting the input json data to a graphlib graph.
Some tests here:

The `json.decode` works fine on this, but it need it on a specific way.
Will keep on working on it with the real json example from the go module.