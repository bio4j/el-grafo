GIT

BASIC
git add <filename>
git add .
git commit -m "Commit message"
git push origin master

ga -A // to remove?

SEE BRANCHES
git branch
git branch -a   // all branches, the ones that are on remote and not on local



CREATE NEW BRANCH
git checkout -b feature_x
git checkout master

git push origin <branch>

RENAME BRANCH
git branch (-m | -M) [<oldbranch>] <newbranch>


DELETE BRANCH REMOTE
git push origin --delete <branchName>


GMT
gmt status
gtm start
gtm stop
gtm list
gtm task feature/topic/foo -m "Task short description"  //new task on branch
gtm comment // comment on current branch+issue related